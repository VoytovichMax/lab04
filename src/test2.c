#include <stdio.h>
int main()
{
    double y, x; 
    int x1, x2, y1; //змінні
    x = 123.456; //Змінна яку ми розбиремо
    x1 = x;	//Права частина
    x2 = 1000 * x;
    x2 = x2 % 1000; //Ліва частина 
    if (x1 == 0) //Перевіряємо ділення на 0
    {
        printf("Ділення на 0!");
    }
    else
    {
    	y = x2 / x1;
    	y1 = 100 * y;
    	y = y1 / 100; //Відповідь
    }
}
